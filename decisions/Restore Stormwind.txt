country_decisions = {

    restore_kingdom_of_stormwind = {
		major = yes
		potential = {
			
			NOT = { exists = STW } # Stormwind
			primary_culture = culture_azerothian
			is_colonial_nation = no
			
		}
        provinces_to_highlight = {
			region = region_elwynn_forest
			OR = {
				NOT = { owned_by = ROOT }
				#is_city = no
			}
		}
		allow = {
			capital_scope = { region = region_elwynn_forest }
		    owns_core_province = 1022 #Stormwind
			NOT = { exists = STW }
			total_development = 300
			stability = 2
			prestige = 60
			mil = 4
			mil_power = 300
			is_at_war = no		
		}
		effect = {
			add_prestige = 25
			change_tag = STW
			change_primary_culture = culture_azerothian
			add_mil_power = -300
			add_country_modifier = {
				name = "centralization_modifier"
				duration = 7300
			}
            
			change_government = monarchy
			set_capital = 1022
			swap_free_idea_group = yes
		}
		
		ai_importance = 300
		
		ai_will_do = {
			factor = 1
		}
	}