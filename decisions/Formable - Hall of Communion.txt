country_decisions = {
    form_hall_of_communion = {
		major = yes
		ai_importance = 300
        
		potential = {
            # Exclusions
			NOT = { exists = HLC }
            
            # Requisites
			tag = G38
		}
        
        provinces_to_highlight = {
            NOT = { owned_by = ROOT }
			region = region_sholazar_basin
		}
        
		allow = {
            # Ownership
			region_sholazar_basin = {
				type = all
				owned_by = ROOT
			}
			
			# General
			is_at_war = no	
			stability = 1
			total_development = 200
            
            # Cost
            adm_power = 200
			dip_power = 200
			mil_power = 200
		}
		effect = {
            # Cost
			add_adm_power = -200
			add_dip_power = -200
            add_mil_power = -200
            
            # Switch
			change_tag = HLC
            
            # Bonus
            add_formable_bonus = yes
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}