# country_decisions = {

	# turn_off_migration = {
		# major = yes
	
		# potential = {
			# OR = {
				# has_reform = expeditionary_tribe_reform
				# has_reform = nomad_state_reform
			# }
			# NOT = { has_country_modifier = disallowed_migration }
		# }
		# allow = {
			# ai = no
		# }
		# effect = {
			# set_country_flag = migration_turned_on
			# add_country_modifier = {
				# name = disallowed_migration
				# duration = -1
			# }
		# }
		# ai_will_do = {
			# factor = 0
		# }
	# }

	# turn_on_migration = {
		# major = yes
	
		# potential = {
			# OR = {
				# has_reform = expeditionary_tribe_reform
				# has_reform = nomad_state_reform
			# }
			# has_country_modifier = disallowed_migration
		# }
		# allow = {
			# ai = no
		# }
		# effect = {
			# clr_country_flag = migration_turned_on
			# remove_country_modifier = disallowed_migration
		# }
		# ai_will_do = {
			# factor = 0
		# }
	# }

# }
