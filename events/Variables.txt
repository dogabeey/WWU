namespace = variables

country_event = {
	id = variables.1
	title = e_l
	desc = e_l
	picture = DARK_PORTAL_CLOSEUP_eventPicture
    
    hidden = yes
    
	trigger = {
		any_country = {
			has_country_modifier = alliance_head
		}
	}
	
	mean_time_to_happen = {
		months = 2
	}
	
	option = {
        name = e_l
		random_country = {
			limit = { has_country_modifier = alliance_head }
			save_global_event_target_as = alliance_head_target
		}
	}
}

country_event = {
	id = variables.2
	title = e_l
	desc = e_l
	picture = DARK_PORTAL_CLOSEUP_eventPicture
    
    hidden = yes
    
	trigger = {
		any_country = {
			has_country_modifier = horde_head
		}
	}
	
	mean_time_to_happen = {
		months = 2
	}
	
	option = {
        name = e_l
		random_country = {
			limit = { has_country_modifier = horde_head }
			save_global_event_target_as = horde_head_target
		}
	}
}