namespace = es_start

country_event = {
	id = es_start.1
	title = es_start.1.t
	desc = es_start.1.d
	picture = ES_START_eventPicture
	#goto = root
	
	is_triggered_only = yes
	
	trigger = {
		NOT = { has_country_flag = es_start_ai_no_flag }
	}
	
	immediate = {
		set_country_flag = es_start_ai_no_flag
		if ={
		limit = {
		NOT = { has_global_flag = trade_controller_selected_flag }
		}
		random_country = {
		limit = {
		ai = no
		}
		set_country_flag = trade_control_flag
		}
		set_global_flag = trade_controller_selected_flag
		}
	}
	
	option = {
		name = es_start.1.a
		#log = "es_start.1.a"	
		add_country_modifier = {
		name = "vassal_autonomy"
		duration = 9125
	    }
	}
}