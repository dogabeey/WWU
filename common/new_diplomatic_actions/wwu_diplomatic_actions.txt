bribe_adminstration = {
	category = covert
	
	alert_index = 40
	alert_tooltip = bribe_adminstration_tooltip
	
	require_acceptance = no # Whether the recipient gets an option to decline
	
	is_visible = {
		always = yes
	}
	is_allowed = {
		years_of_income = 3
		NOT = { is_organization = yes }
		NOT = { has_country_modifier = bribed }
		#has_reform_or_has_subject_with_reform = { ARG1 = trade_company_reform }
		OR = {
			has_reform = trade_company_reform
			any_subject_country = { has_reform = trade_company_reform }
		}
		OR = {
			contract = 1
			is_organization = yes
		}
	}
	on_accept = {
		add_years_of_income = -3
		add_spy_network_from = {
			who = FROM
			value = 20
		}
		add_country_modifier = { 
			name = bribed
			duration = 365
		}
	}
	on_decline = 	{ 
	}
	
	ai_acceptance = { 
	}

	ai_will_do = {
		years_of_income = 4
	}
}
form_raid = {
	category = covert
	
	alert_index = 40
	alert_tooltip = form_raid_tooltip
	
	require_acceptance = no # Whether the recipient gets an option to decline
	
	is_visible = {
		always = yes
	}
	is_allowed = {
		has_spy_network_in = {
			who = FROM
			value = 50
		}
		#has_reform_or_has_subject_with_reform = { ARG1 = independent_guild_reform }
		has_guild_trigger = yes
		FROM = {
			has_country_modifier = is_evil
		}
	}
	on_accept = {
		add_spy_network_in = {
			who = FROM
			value = -50
		}
		FROM = {
			random_owned_province = {
				spawn_rebels = {
					type = adventurer_rebels
					size = 3
					friend = ROOT
				}
			}
		}
	}
	on_decline = 	{ 
	}
	
	ai_acceptance = { 
	}

	ai_will_do = {
		is_enemy = FROM
	}
}
assasinate_ruler = {
	category = covert
	
	alert_index = 40
	alert_tooltip = assasinate_ruler_tooltip
	
	require_acceptance = no # Whether the recipient gets an option to decline
	
	is_visible = {
		always = yes
	}
	is_allowed = {
		has_spy_network_in = {
			who = FROM
			value = 80
		}
		#has_reform_or_has_subject_with_reform = { ARG1 = criminal_organization_reform }
		OR = {
			has_reform = criminal_organization_reform
            has_country_flag = has_criminal_guild
			any_subject_country = { has_reform = criminal_organization_reform }
		}
	}
	on_accept = {
		add_spy_network_in = {
			who = FROM
			value = -80
		}
		FROM = {
			kill_ruler = yes
		}
	}
	on_decline = 	{ 
	}
	
	ai_acceptance = { 
	}

	ai_will_do = {
		is_enemy = FROM
	}
}
recruit_manpower = {
	category = covert
	
	alert_index = 40
	alert_tooltip = recruit_manpower_tooltip
	
	require_acceptance = no # Whether the recipient gets an option to decline
	
	is_visible = {
		always = yes
	}
	is_allowed = {
		has_spy_network_in = {
			who = FROM
			value = 35
		}
		#has_reform_or_has_subject_with_reform = { ARG1 = military_organization_reform }
		OR = {
			has_reform = military_organization_reform
            has_country_flag = has_military_guild
			any_subject_country = { has_reform = military_organization_reform }
		}
		FROM = {
			OR = {
				primary_culture = ROOT
				religion = ROOT
				technology_group = ROOT
			}
		}
	}
	on_accept = {
		add_spy_network_in = {
			who = FROM
			value = -35
		}
		add_yearly_manpower = 0.2
		FROM = { add_yearly_manpower = -0.2 }
	}
	on_decline = 	{ 
	}
	
	ai_acceptance = { 
	}

	ai_will_do = {
		NOT = { manpower_percentage = 0.2 }
	}
}
spread_religion = {
	category = covert
	
	alert_index = 40
	alert_tooltip = spread_religion_tooltip
	
	require_acceptance = no # Whether the recipient gets an option to decline
	
	is_visible = {
		always = yes
	}
	is_allowed = {
		has_spy_network_in = {
			who = FROM
			value = 60
		}
		#has_reform_or_has_subject_with_reform = { ARG1 = religious_cult_reform }
		OR = {
			has_reform = religious_cult_reform
            has_country_flag = has_cult_guild
			any_subject_country = { has_reform = religious_cult_reform }
		}
		FROM = {
			any_owned_province = {
				NOT = { religion = ROOT }
			}
		}
	}
	on_accept = {
		add_spy_network_in = {
			who = FROM
			value = -60
		}
		FROM = {
			random_owned_province = {
				limit = { NOT = { religion = ROOT } }
				change_religion = ROOT
			}
			random_owned_province = {
				limit = { NOT = { religion = ROOT } }
				change_religion = ROOT
			}
		}
	}
	on_decline = 	{ 
	}
	
	ai_acceptance = { 
	}

	ai_will_do = {
		always = yes
	}
}
integrate_same_culture = {
	category = influence
	
	alert_index = 40
	alert_tooltip = integrate_alert_tooltip
	
	require_acceptance = no # Whether the recipient gets an option to decline
	
	is_visible = {
		always = yes
	}
	is_allowed = {
		is_subject = no
		NOT = { has_reform = remnant_basic_reform }
		FROM = {
			is_neighbor_of = ROOT
			primary_culture = ROOT
			NOT = { has_country_flag = being_inherited }
			has_reform = remnant_basic_reform
			has_opinion = {
				who = ROOT
				value = 100
			}
		}
	}
	on_accept = {
		set_country_flag = inheriting
		FROM = {
			set_country_flag = being_inherited
		}
	}
	on_decline = 	{ 
	}
	
	ai_acceptance = { 
	}

	ai_will_do = {
		always = yes
	}
}
stop_integration = {
	category = influence
	
	alert_index = 41
	alert_tooltip = integrate_alert_tooltip
	
	require_acceptance = no # Whether the recipient gets an option to decline
	
	is_visible = {
		has_country_flag = inheriting
		FROM = {
			ai = yes
			primary_culture = ROOT
			has_country_flag = being_inherited
			has_reform = remnant_basic_reform
		}
	}
	is_allowed = {
		has_country_flag = inheriting
		FROM = {
			ai = yes
			primary_culture = ROOT
			has_country_flag = being_inherited
			has_reform = remnant_basic_reform
		}
	}
	on_accept = {
		clr_country_flag = inheriting
		FROM = {
			clr_country_flag = being_inherited
		}
	}
	on_decline = 	{ 
	}
	
	ai_acceptance = { 
	}

	ai_will_do = {
		always = no
	}
}
offer_contract = {
	category = influence
	
	alert_index = 40
	alert_tooltip = offer_contract_tooltip
	
	require_acceptance = yes # Whether the recipient gets an option to decline
	
	is_visible = {
		is_subject = no
		NOT = { is_organization = yes }
		FROM = {
			is_organization = yes
		}
	}
	is_allowed = {
		is_subject = no
		tax_income_percentage = 0.0
		is_at_war = no
		FROM = {
			is_subject = no
			is_at_war = no
			has_opinion = {
				who = ROOT
				value = 0
			}
		}
	}
	on_accept = {
		ROOT = {
			create_subject = {
				subject_type = contract
				subject = FROM
			}
		}
		FROM = {
			country_event = {
				id = contracts.1
				days = 1825
			}
		}
	}
	on_decline = 	{ 
	}
	
	ai_acceptance = {
		add_entry = {
			name = INV_OPINION
			limit = {
				always = yes
			}
			export_to_variable = {
				variable_name = ai_value
				value = opinion
				who = FROM
				with = ROOT
			}
			subtract_variable = { which = ai_value value = 50 }
		}
		add_entry = {
			name = TRUST
			limit = {
				always = yes
			}
			export_to_variable = {
				variable_name = ai_value
				value = trust
				who = FROM
				with = ROOT
			}
			subtract_variable = { which = ai_value value = 40 }
			divide_variable = { which = ai_value value = 3 }
		}
		add_entry = {
			name = DISTANCE
			limit = {
				always = yes
			}
			export_to_variable = {
				variable_name = ai_value
				value = border_distance
				who = FROM
				with = ROOT
			}
			subtract_variable = { which = ai_value value = 50 }
			divide_variable = { which = ai_value value = 3 }
			multiply_variable = { which = ai_value value = -1 }
		}
		add_entry = {
			name = HAVE_LOANS
			limit = {
				FROM = { num_of_loans = 1 }
				FROM = { NOT = { num_of_loans = 2 } }
			}
			export_to_variable = {
				which = ai_value
				value = 10
			}
		}
		add_entry = {
			name = HAVE_LOANS
			limit = {
				FROM = { num_of_loans = 2 }
				FROM = { NOT = { num_of_loans = 3 } }
			}
			export_to_variable = {
				which = ai_value
				value = 20
			}
		}
		add_entry = {
			name = HAVE_LOANS
			limit = {
				FROM = { num_of_loans = 3 }
				FROM = { NOT = { num_of_loans = 4 } }
			}
			export_to_variable = {
				which = ai_value
				value = 30
			}
		}
	}

	ai_will_do = {
		NOT = { contract = 1 }
		tax_income_percentage = 0.2
	}
}

suggest_contract = {
	category = influence
	
	alert_index = 40
	alert_tooltip = suggest_contract_tooltip
	
	require_acceptance = yes # Whether the recipient gets an option to decline
	
	is_visible = {
		is_subject = no
		is_organization = yes
		FROM = {
			is_subject = no
			NOT = { is_organization = yes }
		}
	}
	is_allowed = {
		is_subject = no
		is_at_war = no
		FROM = {
			is_subject = no
			is_at_war = no
			has_opinion = {
				who = ROOT
				value = 0
			}
		}
	}
	on_accept = {
		FROM = {
			create_subject = {
				subject_type = contract
				subject = ROOT
			}
		}
		ROOT = {
			country_event = {
				id = contracts.1
				days = 1825
			}
		}
	}
	on_decline = 	{ 
	}
	
	ai_acceptance = {
		add_entry = {
			name = INV_OPINION
			limit = {
				always = yes
			}
			export_to_variable = {
				variable_name = ai_value
				value = opinion
				who = ROOT
				with = FROM
			}
			subtract_variable = { which = ai_value value = 50 }
		}
		add_entry = {
			name = TRUST
			limit = {
				always = yes
			}
			export_to_variable = {
				variable_name = ai_value
				value = trust
				who = ROOT
				with = FROM
			}
			subtract_variable = { which = ai_value value = 40 }
			divide_variable = { which = ai_value value = 3 }
		}
		add_entry = {
			name = DISTANCE
			limit = {
				always = yes
			}
			export_to_variable = {
				variable_name = ai_value
				value = border_distance
				who = FROM
				with = ROOT
			}
			subtract_variable = { which = ai_value value = 50 }
			divide_variable = { which = ai_value value = 3 }
			multiply_variable = { which = ai_value value = -1 }
		}
		add_entry = {
			name = HAVE_LOANS
			limit = {
				FROM = { num_of_loans = 1 }
				FROM = { NOT = { num_of_loans = 2 } }
			}
			export_to_variable = {
				which = ai_value
				value = -20
			}
		}
		add_entry = {
			name = HAVE_LOANS
			limit = {
				FROM = { num_of_loans = 2 }
				FROM = { NOT = { num_of_loans = 3 } }
			}
			export_to_variable = {
				which = ai_value
				value = -30
			}
		}
		add_entry = {
			name = HAVE_LOANS
			limit = {
				FROM = { num_of_loans = 3 }
				FROM = { NOT = { num_of_loans = 4 } }
			}
			export_to_variable = {
				which = ai_value
				value = -40
			}
		}
	}

	ai_will_do = {
		NOT = { contract = 1 }
		tax_income_percentage = 0.2
	}
}

invite_to_alliance = {
	category = alliance
	
	alert_index = 40
	alert_tooltip = invite_to_alliance_tooltip
	
	require_acceptance = yes # Whether the recipient gets an option to decline
	
	is_visible = {
		always = yes
		has_country_modifier = alliance_head
		FROM = {
			is_subject = no
		}
	}
	is_allowed = {
		is_subject = no
		FROM = {
			is_subject = no
			NOT = { has_country_modifier = recently_rejected_alliance }
			NOT = { has_country_modifier = recently_rejected_alliance_request }
			NOT = { has_country_modifier = is_evil }
			total_development = 75
		}
	}
	on_accept = {
		ROOT = {
			create_subject = {
				subject_type = faction_alliance
				subject = FROM
			}
		}
	}
	on_decline = 	{ 
	}
	
	ai_acceptance = {
		add_entry = {
			name = INV_OPINION
			limit = {
				always = yes
			}
			export_to_variable = {
				variable_name = ai_value
				value = opinion
				who = FROM
				with = ROOT
			}
			subtract_variable = { which = ai_value value = 100 }
		}
		add_entry = {
			name = INV_CIVILIZED
			limit = {
				FROM = { NOT = { is_civilized = yes } }
			}
			export_to_variable = {
				which = ai_value
				value = -50
			}
		}
		add_entry = {
			name = INV_LESSER
			limit = {
				is_lesser = yes
			}
			export_to_variable = {
				which = ai_value
				value = -1000
			}
		}
		add_entry = {
			name = INV_TOOMANYVASSAL
			limit = {
				faction_alliance = 6
			}
			export_to_variable = {
				which = ai_value
				value = -100
			}
		}
		add_entry = {
			name = INV_FARTOOMANYVASSAL
			limit = {
				faction_alliance = 8
			}
			export_to_variable = {
				which = ai_value
				value = -175
			}
		}
		add_entry = {
			name = INV_FARFARTOOMANYVASSAL
			limit = {
				faction_alliance = 10
			}
			export_to_variable = {
				which = ai_value
				value = -225
			}
		}
		add_entry = {
			name = INV_HISTORICALALLIANCE
			limit = {
				FROM = { historical_alliance = yes }
			}
			export_to_variable = {
				which = ai_value
				value = 150
			}
		}
		add_entry = {
			name = INV_HISTORICALHORDE
			limit = {
				FROM = { historical_horde = yes }
			}
			export_to_variable = {
				which = ai_value
				value = -250
			}
		}
		add_entry = {
			name = INV_NOTHASALLIANCEFRIEND
			limit = {
				NOT = { 
					any_country = {
						OR = {
							is_subject_of_type = faction_alliance
							has_country_modifier = alliance_head
						}
						alliance_with = FROM
					} 
				}
			}
			export_to_variable = {
				which = ai_value
				value = -200
			}
		}
		add_entry = {
			name = INV_HASHORDEFRIEND
			limit = {
				any_country = {
					OR = {
						is_subject_of_type = faction_horde
						has_country_modifier = horde_head
					}
					alliance_with = FROM
				}
			}
			export_to_variable = {
				which = ai_value
				value = -100
			}
		}
		add_entry = {
			name = INV_NEVERALLIANCE
			limit = {
				FROM = {
					never_alliance = yes
				}
			}
			export_to_variable = {
				which = ai_value
				value = -1000
			}
		}
	}

	ai_will_do = {
		always = yes
	}
}
invite_to_horde = {
	category = alliance
	
	alert_index = 40
	alert_tooltip = invite_to_alliance_tooltip
	
	require_acceptance = yes # Whether the recipient gets an option to decline
	
	is_visible = {
		always = yes
		FROM = {
			is_subject = no
		}
		has_country_modifier = horde_head
	}
	is_allowed = {
		is_subject = no
		FROM = {
			is_subject = no
			NOT = { has_country_modifier = is_evil }
			NOT = { has_country_modifier = recently_rejected_horde }
			NOT = { has_country_modifier = recently_rejected_horde_request }
			total_development = 75
		}
	}
	on_accept = {
		ROOT = {
			create_subject = {
				subject_type = faction_horde
				subject = FROM
			}
		}
	}
	on_decline = 	{ 
	}
	
	ai_acceptance = {
		add_entry = {
			name = INV_OPINION
			limit = {
				always = yes
			}
			export_to_variable = {
				variable_name = ai_value
				value = opinion
				who = FROM
				with = ROOT
			}
			subtract_variable = { which = ai_value value = 100 }
		}
		add_entry = {
			name = INV_LESSER
			limit = {
				is_lesser = yes
			}
			export_to_variable = {
				which = ai_value
				value = -1000
			}
		}
		add_entry = {
			name = INV_TOOMANYVASSAL
			limit = {
				faction_horde = 6
			}
			export_to_variable = {
				which = ai_value
				value = -100
			}
		}
		add_entry = {
			name = INV_FARTOOMANYVASSAL
			limit = {
				faction_horde = 8
			}
			export_to_variable = {
				which = ai_value
				value = -175
			}
		}
		add_entry = {
			name = INV_FARFARTOOMANYVASSAL
			limit = {
				faction_horde = 10
			}
			export_to_variable = {
				which = ai_value
				value = -225
			}
		}
		add_entry = {
			name = INV_SAVAGE
			limit = {
				FROM = { NOT = { is_savage = yes } }
			}
			export_to_variable = {
				which = ai_value
				value = -50
			}
		}
		add_entry = {
			name = INV_HISTORICALALLIANCE
			limit = {
				FROM = { historical_alliance = yes }
			}
			export_to_variable = {
				which = ai_value
				value = -1000
			}
		}
		add_entry = {
			name = INV_HISTORICALHORDE
			limit = {
				FROM = { historical_horde = yes }
			}
			export_to_variable = {
				which = ai_value
				value = 50
			}
		}
		add_entry = {
			name = INV_HASALLIANCEFRIEND
			limit = {
				any_country = {
					OR = {
						is_subject_of_type = faction_alliance
						has_country_modifier = alliance_head
					}
					alliance_with = FROM
				}
			}
			export_to_variable = {
				which = ai_value
				value = -100
			}
		}
		add_entry = {
			name = INV_NOTHASHORDEFRIEND
			limit = {
				NOT = { any_country = {
					OR = {
						is_subject_of_type = faction_horde
						has_country_modifier = horde_head
					}
					alliance_with = FROM
				} }
			}
			export_to_variable = {
				which = ai_value
				value = -200
			}
		}
		add_entry = {
			name = INV_NEVERHORDE
			limit = {
				FROM = {
					never_horde = yes
				}
			}
			export_to_variable = {
				which = ai_value
				value = -1000
			}
		}
	}

	ai_will_do = {
		always = yes
	}
}
join_the_alliance = {
	category = alliance
	
	alert_index = 41
	alert_tooltip = join_the_alliance_tooltip
	
	require_acceptance = yes # Whether the recipient gets an option to decline
	
	is_visible = {
		always = yes
		is_subject = no
		FROM = {
			is_subject = no
			has_country_modifier = alliance_head
		}
	}
	is_allowed = {
		is_subject = no
		NOT = { has_country_modifier = is_evil }
		NOT = { has_country_modifier = recently_rejected_alliance }
		NOT = { has_country_modifier = recently_rejected_alliance_request }
		total_development = 75
}
	on_accept = {
		FROM = {
			create_subject = {
				subject_type = faction_alliance
				subject = ROOT
			}
		}
	}
	on_decline = 	{ 
	}
	
	ai_acceptance = {
		add_entry = {
			name = INV_OPINION
			limit = {
				always = yes
			}
			export_to_variable = {
				variable_name = ai_value
				value = opinion
				who = FROM
				with = ROOT
			}
			subtract_variable = { which = ai_value value = 100 }
		}
		add_entry = {
			name = INV_CIVILIZED
			limit = {
				NOT = { is_civilized = yes }
			}
			export_to_variable = {
				which = ai_value
				value = -50
			}
		}
		add_entry = {
			name = INV_LESSER
			limit = {
				is_lesser = yes
			}
			export_to_variable = {
				which = ai_value
				value = -1000
			}
		}
		add_entry = {
			name = INV_TOOMANYVASSAL
			limit = {
				FROM = {
					faction_alliance = 6
				}
			}
			export_to_variable = {
				which = ai_value
				value = -100
			}
		}
		add_entry = {
			name = INV_FARTOOMANYVASSAL
			limit = {
				FROM = {
					faction_alliance = 8
				}
			}
			export_to_variable = {
				which = ai_value
				value = -175
			}
		}
		add_entry = {
			name = INV_FARFARTOOMANYVASSAL
			limit = {
				FROM = {
					faction_alliance = 10
				}
			}
			export_to_variable = {
				which = ai_value
				value = -225
			}
		}
		add_entry = {
			name = INV_HISTORICALALLIANCE
			limit = {
				historical_alliance = yes
			}
			export_to_variable = {
				which = ai_value
				value = 150
			}
		}
		add_entry = {
			name = INV_HISTORICALHORDE
			limit = {
				historical_horde = yes
			}
			export_to_variable = {
				which = ai_value
				value = -250
			}
		}
		add_entry = {
			name = INV_HASHORDEFRIEND
			limit = {
				any_country = {
					OR = {
						is_subject_of_type = faction_horde
						has_country_modifier = horde_head
					}
					alliance_with = ROOT
				}
			}
			export_to_variable = {
				which = ai_value
				value = -100
			}
		}
		add_entry = {
			name = INV_NOTHASALLIANCEFRIEND
			limit = {
				NOT = { any_country = {
					OR = {
						is_subject_of_type = faction_alliance
						has_country_modifier = alliance_head
					}
					alliance_with = ROOT
				} }
			}
			export_to_variable = {
				which = ai_value
				value = -200
			}
		}
		add_entry = {
			name = INV_NEVERALLIANCE
			limit = {
				never_alliance = yes
			}
			export_to_variable = {
				which = ai_value
				value = -1000
			}
		}
	}

	ai_will_do = {
		always = yes
	}
}
join_the_horde = {
	category = alliance
	
	alert_index = 41
	alert_tooltip = join_the_alliance_tooltip
	
	require_acceptance = yes # Whether the recipient gets an option to decline
	
	is_visible = {
		always = yes
		is_subject = no
		FROM = {
			is_subject = no
			has_country_modifier = horde_head
		}
	}
	is_allowed = {
		is_subject = no
		NOT = { has_country_modifier = is_evil }
		NOT = { has_country_modifier = recently_rejected_horde }
		NOT = { has_country_modifier = recently_rejected_horde_request }
		total_development = 75
	}
	on_accept = {
		FROM = {
			create_subject = {
				subject_type = faction_horde
				subject = ROOT
			}
		}
	}
	on_decline = 	{ 
	}
	
	ai_acceptance = {
		add_entry = {
			name = INV_OPINION
			limit = {
				always = yes
			}
			export_to_variable = {
				variable_name = ai_value
				value = opinion
				who = FROM
				with = ROOT
			}
			subtract_variable = { which = ai_value value = 100 }
		}
		add_entry = {
			name = INV_SAVAGE
			limit = {
				NOT = { is_savage = yes }
			}
			export_to_variable = {
				which = ai_value
				value = -50
			}
		}
		add_entry = {
			name = INV_LESSER
			limit = {
				is_lesser = yes
			}
			export_to_variable = {
				which = ai_value
				value = -1000
			}
		}
		add_entry = {
			name = INV_TOOMANYVASSAL
			limit = {
				FROM = {
					faction_horde = 6
				}
			}
			export_to_variable = {
				which = ai_value
				value = -100
			}
		}
		add_entry = {
			name = INV_FARTOOMANYVASSAL
			limit = {
				FROM = {
					faction_horde = 8
				}
			}
			export_to_variable = {
				which = ai_value
				value = -175
			}
		}
		add_entry = {
			name = INV_FARFARTOOMANYVASSAL
			limit = {
				FROM = {
					faction_horde = 10
				}
			}
			export_to_variable = {
				which = ai_value
				value = -225
			}
		}
		add_entry = {
			name = INV_HISTORICALALLIANCE
			limit = {
				historical_alliance = yes
			}
			export_to_variable = {
				which = ai_value
				value = -1000
			}
		}
		add_entry = {
			name = INV_HISTORICALHORDE
			limit = {
				historical_horde = yes
			}
			export_to_variable = {
				which = ai_value
				value = 50
			}
		}
		add_entry = {
			name = INV_HASALLIANCEFRIEND
			limit = {
				any_country = {
					OR = {
						is_subject_of_type = faction_alliance
						has_country_modifier = alliance_head
					}
					alliance_with = ROOT
				}
			}
			export_to_variable = {
				which = ai_value
				value = -100
			}
		}
		add_entry = {
			name = INV_NOTHASHORDEFRIEND
			limit = {
				NOT = { any_country = {
					OR = {
						is_subject_of_type = faction_horde
						has_country_modifier = horde_head
					}
					alliance_with = ROOT
				} }
			}
			export_to_variable = {
				which = ai_value
				value = -200
			}
		}
		add_entry = {
			name = INV_NEVERHORDE
			limit = {
				never_horde = yes
			}
			export_to_variable = {
				which = ai_value
				value = -1000
			}
		}
	}

	ai_will_do = {
		always = yes
	}
}

kick_from_alliance = {
	category = alliance
	
	alert_index = 40
	alert_tooltip = kick_from_alliance_tooltip
	
	require_acceptance = no # Whether the recipient gets an option to decline
	
	is_visible = {
		always = yes
		has_country_modifier = alliance_head
		FROM = {
			is_subject_of_type = faction_alliance
		}
	}
	is_allowed = {
		NOT = { prestige = 0 }
	}
	on_accept = {
		FROM = {
			add_country_modifier = {
				name = recently_rejected_alliance_request
				duration = 365
			}
			country_event = { id = factions.1 }
			grant_independence = yes
		}
	}
	on_decline = 	{ 
	}
	
	ai_acceptance = {
	}

	ai_will_do = {
		OR = {
			FROM = {
				liberty_desire = 100
				army_strength = {
					who = ROOT
					value = 1
				}
			}
			FROM = {
				liberty_desire = 75
				army_strength = {
					who = ROOT
					value = 1.25
				}
			}
			FROM = {
				liberty_desire = 50
				army_strength = {
					who = ROOT
					value = 1.50
				}
			}
		}
	}
}

kick_from_horde = {
	category = alliance
	
	alert_index = 40
	alert_tooltip = kick_from_horde_tooltip
	
	require_acceptance = no # Whether the recipient gets an option to decline
	
	is_visible = {
		always = yes
		has_country_modifier = horde_head
		FROM = {
			is_subject_of_type = faction_horde
		}
	}
	is_allowed = {
		NOT = { prestige = 0 }
	}
	on_accept = {
		FROM = {
			add_country_modifier = {
				name = recently_rejected_horde_request
				duration = 365
			}
			country_event = { id = factions.1 }
			grant_independence = yes
		}
	}
	on_decline = 	{ 
	}
	
	ai_acceptance = {
	}

	ai_will_do = {
		OR = {
			FROM = {
				liberty_desire = 100
				army_strength = {
					who = ROOT
					value = 1
				}
			}
			FROM = {
				liberty_desire = 75
				army_strength = {
					who = ROOT
					value = 1.25
				}
			}
			FROM = {
				liberty_desire = 50
				army_strength = {
					who = ROOT
					value = 1.50
				}
			}
		}
	}
}